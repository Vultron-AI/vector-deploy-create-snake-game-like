# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-create-snake-game-like
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
